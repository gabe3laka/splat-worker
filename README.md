# splat-worker
RunPod serverless worker that trains a 3D Gaussian Splat (gsplat + COLMAP) from a phone scan video and uploads scene.ply to Supabase. Sibling to lingbot-map-worker. Output is consumed by SuperSplat in the browser.
